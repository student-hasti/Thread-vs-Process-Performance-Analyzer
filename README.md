# Thread-vs-Process-Performance-Analyzer
This project compares sequential, multithreading, and multiprocessing execution in C. It performs tasks like array operations, prime generation, and matrix multiplication, measuring execution time for each method to determine the most efficient approach and demonstrate the benefits of parallel processing.
